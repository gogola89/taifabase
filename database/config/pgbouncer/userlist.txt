; PgBouncer User Authentication List
; Format: "username" "password"
; Password format: md5<md5 hash of password+username>
; Example: For password "mypassword" and username "myuser", the hash is: md5<md5(mypasswordmyuser)>
;
; IMPORTANT: This file contains sensitive credentials
; In production, use Docker secrets or external authentication methods
; Never commit actual production passwords to version control

; Development user credentials
; Username: taifabase_user
; Password: Plain text for SCRAM-SHA-256 authentication
; Day 2 Fix: Plain text password for SCRAM support (PgBouncer will handle SCRAM negotiation)
"taifabase_user" "taifabase_dev_password"
